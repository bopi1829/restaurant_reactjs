/*
 * MenuPage Messages
 *
 * This contains all the text for the MenuPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.MenuPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Menu du restaurant, La Bonne Table',
  },
});
